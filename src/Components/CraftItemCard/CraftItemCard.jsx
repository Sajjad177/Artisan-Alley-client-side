
import PropTypes from 'prop-types';

const CraftItemCard = () => {
    return (
        <div>
            
        </div>
    );
};

CraftItemCard.propTypes = {
    
};

export default CraftItemCard;